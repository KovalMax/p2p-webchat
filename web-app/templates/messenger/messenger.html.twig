{% extends 'base.html.twig' %}

{% block title %}{{ 'Messenger'|trans }}{% endblock %}

{% block body %}
    <div class="col">
        <ul class="list-group">
            {% if messages|default %}
                {# @var message \App\Entity\Message #}
                {% for message in messages %}
                    <li class="list-group-item">
                        <p>{{ message.user.firstName }}</p><p>{{ message.message }}</p>
                    </li>
                {% endfor %}
            {% endif %}
        </ul>
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
{% endblock %}