{% extends 'base.html.twig' %}

{% block title %}{{ 'registration'|trans }}{% endblock %}

{% block body %}
    <h4 class="mx-auto align-self-center mb-4">{{'registration'|trans}}</h4>
    <div class="container">
        <div class="col-6 mx-auto">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}